---
"@gis-engine/cli": minor
---

Make the generated `app` template `Reload map.json` action re-read `./map.json`
from disk with no-store fetch semantics, then validate and render the updated
spec without a browser refresh.
