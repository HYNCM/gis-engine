---
"@gis-engine/cli": minor
---

Write `artifact-manifest.json` during `create-gis-map --generate` with generated
file roles, required-review flags, byte sizes, and sha256 hashes for
reproducible CI/reviewer handoff.
