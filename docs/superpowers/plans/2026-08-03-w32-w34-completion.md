# W32-W34 Compatibility And Evidence Integrity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close milestone issues #41, #43, #40, #38, #42, and #39 with executable evidence while preserving the current MCP, MapLibre, SceneView3D, Workbench, PMTiles, and GeoParquet promotion boundaries.

**Architecture:** Treat each issue as an independently reviewable contract slice. Put durable policy in structured repository files, keep workflows as consumers, keep compatibility decisions in dated quality reports, and make tests prove both the positive path and the fail-closed boundary before implementation changes land.

**Tech Stack:** TypeScript 5.9, Node.js ESM scripts, TypeBox/Ajv, Vitest, Playwright, pnpm workspaces, GitHub Actions YAML, Markdown evidence artifacts.

---

### Task 1: Public v1.5 release truth (#41)

**Files:**
- Modify: `tests/docs/public-docs-consistency.test.ts`
- Modify: `docs/website/release-notes.md`
- Create: `docs/reviews/release-truth-quality-decision-2026-08-03.md`

- [ ] **Step 1: Write failing public-doc tests**

Add `docs/website/release-notes.md` to the current-version and canonical-inventory file lists. Add assertions that the current section contains `v1.5.0`, all 14 ordered tool names, and the explicit No-go wording for hosted Workbench, stable SceneView3D, and PMTiles runtime query.

- [ ] **Step 2: Verify RED**

Run `pnpm vitest run tests/docs/public-docs-consistency.test.ts`. Expect failure because the page only describes v1.0.0 and seven tools.

- [ ] **Step 3: Implement the release index**

Prepend a v1.5.0 section that links to the canonical MCP overview and lists the ordered inventory once. Preserve the complete v1.0.0 history, change its package tags to historical-at-release wording, and state all three promotion exclusions without claiming a hosted or runtime capability.

- [ ] **Step 4: Verify GREEN and record HOC-N3**

Run `pnpm test:docs`, `node scripts/doc-generator.mjs links`, and `pnpm release:preflight` under the pinned Node 22 runtime. Record command evidence and a pass/block decision in the quality report.

- [ ] **Step 5: Commit**

Commit only the Task 1 files with `docs: align v1.5 release truth`.

### Task 2: Specialist evidence freshness and recovery deduplication (#43)

**Files:**
- Create: `scripts/agent-evidence.mjs`
- Create: `scripts/recovery-incident.mjs`
- Create: `scripts/git-push-retry.mjs`
- Modify: `scripts/agent-runner.mjs`
- Modify: `scripts/sla-checker.mjs`
- Modify: `scripts/handoff-ledger.mjs`
- Modify: `.github/workflows/agent-daily.yml`
- Modify: `.github/workflows/agent-weekly.yml`
- Modify: `.github/workflows/agent-monthly.yml`
- Modify: `.github/workflows/agent-failure-recovery.yml`
- Modify: `tests/framework/agent-framework.test.ts`
- Create: `tests/framework/recovery-incident.test.ts`
- Create: `tests/framework/git-push-retry.test.ts`
- Create: `docs/reviews/agent-recovery-incident-triage-2026-08-03.md`
- Create: `docs/reviews/evidence-integrity-quality-decision-2026-08-03.md`

- [ ] **Step 1: Write failing provenance tests**

Add tests where a fresh report with `evidence_kind: template` remains stale for specialist SLA/HOC checks, while `evidence_kind: specialist` satisfies them. Assert a stable diagnostic such as `EVIDENCE.TEMPLATE_NOT_SPECIALIST` is returned instead of silently accepting the report.

- [ ] **Step 2: Verify provenance RED**

Run `pnpm vitest run tests/framework/agent-framework.test.ts`. Expect the template fixture to be accepted by at least one current freshness path.

- [ ] **Step 3: Centralize evidence classification**

Implement one parser/classifier in `scripts/agent-evidence.mjs` and consume it from runner, SLA, and handoff code. Cadence workflows must retain explicit `evidence_kind: template` output and run the SLA/escalation check after generation.

- [ ] **Step 4: Write failing incident identity tests**

Test that `{workflow, failedRunId}` produces a deterministic marker such as `<!-- agent-recovery:Daily:29710687953 -->`, that matching issues are updated or reopened, and that a different run gets a different identity.

- [ ] **Step 5: Verify incident RED and implement deduplication**

Run `pnpm vitest run tests/framework/recovery-incident.test.ts`, then implement pure identity/body helpers. Update the workflow to search open and closed issues by marker before calling create; comment/update the canonical issue and reopen it when necessary.

- [ ] **Step 6: Write failing concurrent-push tests and implement bounded retry**

Test a first non-fast-forward result followed by a successful fetch/rebase/push result, plus terminal failure after the configured bound. Implement the retry helper and route scheduled planning writes through it without force-push.

- [ ] **Step 7: Record historical triage and quality decision**

Document #32-#35 as duplicate records for run `29710687953`, name the canonical issue, and recommend closure only after the fix is on the default branch. Do not close them during implementation review.

- [ ] **Step 8: Verify and commit**

Run `pnpm test:agent-framework`, `node scripts/sla-checker.mjs --period 2026-08-03`, `node scripts/handoff-ledger.mjs --check --dry-run`, `pnpm knip`, and `pnpm check`. Commit with `fix: make agent evidence actionable`.

### Task 3: MCP 2026-07-28 compatibility gate (#40)

**Files:**
- Create: `docs/planning/feature-specs/mcp-2026-07-28-compatibility.md`
- Create: `tests/fixtures/mcp/compatibility-2026-07-28.json`
- Create: `tests/ai/mcp-2026-07-28-compatibility.test.ts`
- Create: `docs/reviews/mcp-2026-07-28-quality-decision-2026-08-03.md`
- Modify: `package.json`

- [ ] **Step 1: Write the fixture contract and failing test**

Define a JSON fixture whose current default is `2025-11-25`, candidate is `2026-07-28`, decision is `no-go`, and coverage keys are `discovery`, `transportLifecycle`, `resultType`, `cacheMetadata`, `subscriptions`, `extensions`, and `oldClientBehavior`. Test that the fixture is complete, that `GIS_ENGINE_MCP_PROTOCOL_VERSION` remains `2025-11-25`, and that the canonical 14 tool descriptors still validate through the SDK `ListToolsResultSchema`.

- [ ] **Step 2: Verify RED**

Run `pnpm vitest run tests/ai/mcp-2026-07-28-compatibility.test.ts`. Expect missing fixture/RFC failures.

- [ ] **Step 3: Write the no-default-change RFC and quality decision**

Map every new protocol behavior to `supported`, `blocked`, or `not-applicable`, cite the official stable spec and available TypeScript SDK v2 evidence, define rollback as retaining SDK 1.x plus `2025-11-25`, and explicitly prohibit tool/order/schema/result fallback drift.

- [ ] **Step 4: Add and run the compatibility command**

Add `test:compat:mcp` to run the new Vitest file. Run `pnpm build:schema`, `pnpm test:compat:mcp`, `pnpm test:ai`, `pnpm test:docs`, and `pnpm check`.

- [ ] **Step 5: Commit**

Commit with `test: gate MCP 2026-07-28 compatibility`.

### Task 4: Stable MapLibre 6.1 compatibility gate (#38)

**Files:**
- Modify: `scripts/maplibre-compat-matrix.mjs`
- Modify: `tests/framework/maplibre-compat-matrix.test.ts`
- Modify: `.github/workflows/ci.yml`
- Create: `docs/reviews/maplibre-5.24-6.1-builder-evidence-2026-08-03.md`
- Create: `docs/reviews/maplibre-6.1-quality-decision-2026-08-03.md`

- [ ] **Step 1: Write failing exact-version tests**

Change expected matrix entries to exactly `5.24.0` and `6.1.0`. Assert the generated fixture covers ESM import, explicit worker/CSP setup for v6, event/image callbacks, overscaled vector query behavior, `queryRenderedFeatures`, generated application build, browser evidence, and performance metadata.

- [ ] **Step 2: Verify RED**

Run `pnpm vitest run tests/framework/maplibre-compat-matrix.test.ts`. Expect the old `6.0.0-22` matrix and workflow values to fail.

- [ ] **Step 3: Extend the matrix and CI**

Keep 5.24.0 as the package/default baseline. Update only the candidate matrix entry and generated evidence fields; do not change dependency ranges or application defaults. Make the report record browser engine, render/query timings, import form, worker path, and observed behavior for both versions.

- [ ] **Step 4: Run real dual-version evidence**

Run `pnpm test:compat:maplibre`, `pnpm test:adapter`, `pnpm test:e2e:browser`, and `GIS_ENGINE_REQUIRE_VISUAL_SNAPSHOT=1 pnpm test:snapshot:visual`. Record the exact results and issue a separate keep/bump decision.

- [ ] **Step 5: Commit**

Commit with `test: gate MapLibre 6.1 compatibility`.

### Task 5: GeoParquet 1.1 and 2.0 RC readiness boundary (#42)

**Files:**
- Modify: `packages/engine/src/spec/cloud-native/geoparquet-source.ts`
- Modify: `packages/engine/src/spec/cloud-native/validate.ts`
- Create: `tests/fixtures/geoparquet/valid-1.1.json`
- Create: `tests/fixtures/geoparquet/valid-2.0-rc.1.json`
- Create: `tests/fixtures/geoparquet/invalid-ambiguous.json`
- Modify: `tests/schema/cloud-native-policy.test.ts`
- Modify: `docs/planning/feature-specs/cloud-native-source-readiness.md`
- Create: `docs/reviews/geoparquet-version-boundary-quality-decision-2026-08-03.md`

- [ ] **Step 1: Write failing versioned metadata tests**

Add fixtures and tests for explicit spec version, encoding/logical type, CRS, row-group statistics, and 1.1 covering. Assert stable diagnostics for unsupported version, missing version, 1.1-only fields under 2.0 RC, 2.0-only fields under 1.1, and ambiguous metadata. Assert every path still emits the runtime-blocked diagnostic and performs no IO.

- [ ] **Step 2: Verify RED**

Run `pnpm vitest run tests/schema/cloud-native-policy.test.ts`. Expect schema rejection or missing version diagnostics.

- [ ] **Step 3: Extend the TypeBox contract and validator**

Use a discriminated metadata union for `1.1.0` and `2.0.0-rc.1`; keep URL/resource policy unchanged. Add stable `GEOPARQUET.VERSION_*` and `GEOPARQUET.METADATA_*` diagnostics while retaining `CAPABILITY.RUNTIME_BLOCKED`.

- [ ] **Step 4: Document and verify the No-go boundary**

Update readiness docs with the field matrix and explicit prohibition on fetch, parser, WASM, worker, and runtime query. Run `pnpm build:schema`, `pnpm test:schema`, `pnpm test:resources`, `pnpm test:docs`, and `pnpm check`.

- [ ] **Step 5: Commit**

Commit with `feat: model GeoParquet version boundaries`.

### Task 6: Single structured bundle budget (#39)

**Files:**
- Create: `config/package-size-budgets.json`
- Create: `scripts/package-size-policy.mjs`
- Modify: `scripts/check-bundle-size.mjs`
- Modify: `.github/workflows/bundle-size.yml`
- Modify: `docs/engineering/contract-freeze.md`
- Create: `tests/framework/package-size-policy.test.ts`
- Create: `docs/reviews/package-size-budget-quality-decision-2026-08-03.md`

- [ ] **Step 1: Measure current artifacts and write failing policy tests**

Build packages, record current engine and CLI gzip sizes, then choose budgets that preserve the frozen contract unless evidence requires an explicit approved increase. Test that the JSON source has byte values and `blocking` semantics, the checker imports it, the workflow calls the checker without inline byte limits, and docs render the same human-readable limits.

- [ ] **Step 2: Verify RED**

Run `pnpm vitest run tests/framework/package-size-policy.test.ts`. Expect failures from the current inline 170KB/70KB workflow limits and absent structured source.

- [ ] **Step 3: Implement one policy consumer**

Make `pnpm size:check` read the JSON policy, measure gzip bytes, print measured/budget values, and exit nonzero only for entries marked blocking. Remove workflow numeric duplication and make the documentation consistency test parse the same source.

- [ ] **Step 4: Verify and commit**

Run `pnpm size:check`, `pnpm test:agent-framework`, `pnpm test:docs`, and `pnpm check`. Commit with `fix: centralize package size budgets`.

### Task 7: Integrated quality, planning, and remote closure

**Files:**
- Create: `docs/reviews/w32-w34-compatibility-evidence-closeout-2026-08-03.md`
- Modify: `docs/planning/next-step-plan.md`
- Modify: `docs/planning/weekly-digest.md`
- Modify: `docs/planning/monthly-roadmap.md`
- Modify: `docs/planning/task-burndown.md`
- Modify: `docs/planning/dependency-graph.md`
- Generate: `docs/planning/issues-snapshot.md`
- Generate: `docs/planning/handoff-ledger.json`
- Generate: `docs/planning/AGENT_HEALTH_DASHBOARD.md`

- [ ] **Step 1: Run the complete local gate plan**

Run `pnpm build:schema`, `pnpm check`, `pnpm test:compat:mcp`, `pnpm test:compat:maplibre`, `pnpm test:e2e:browser`, strict visual snapshots, `pnpm size:check`, `pnpm knip`, docs links, HOC checks, and `git diff --check`.

- [ ] **Step 2: Obtain final @quality review**

Audit every Issue acceptance item against a file, test, command result, or explicit No-go decision. Fix all critical/important findings and rerun affected gates.

- [ ] **Step 3: Publish branch and validate final HEAD**

Push `codex/w32-w34-completion`, open a PR referencing all six issues, wait for required checks, and fix failures without weakening gates.

- [ ] **Step 4: Merge and close remote state**

After final-HEAD CI passes, merge the PR, close #38-#43 with evidence, and close the milestone only when it reports zero open issues. Triage #32-#35 according to the recorded canonical-incident decision.

- [ ] **Step 5: Refresh planning surfaces together**

Run authenticated planning evidence generation after issue closure, update the closeout report, and verify snapshot, digest, roadmap, burndown, dependency graph, handoff ledger, and dashboard all cite the same final revision/evidence run.
